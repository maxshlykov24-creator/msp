# Приёмка Модуля 2 · 2026-08-19

Проверка выполнена Аудитором сразу после установки. Метод: сверка заявленного в `_СИСТЕМА/РЕЕСТР_КОМАНДЫ.md` с фактом на диске, а не с планом.

---

## Светофор 1 · Структура организации 🟢

| Проверка | Ожидалось | Факт |
|---|---|---|
| Отделов с `РУКОВОДИТЕЛЬ.md` | 12 | 12 |
| Отделов с `БАЗА_СМЫСЛОВ.md` | 12 | 12 |
| Отделов с `ВХОДЯЩЕЕ/` | 12 | 12 |
| Реестр совпадает с диском | один в один | совпадает, включая пути с пробелами |
| Файлов клиентов перемещено | 0 | 0 |
| Новых папок | 1 (`3.10. Устойчивость`) | 1 |

Отклонение от плана: зона устойчивости получила номер **3.10**, а не 3.8 — номера 3.8 и 3.9 заняты Хотелками и Вопросами. Перенумерация сломала бы существующие ссылки.

## Светофор 2 · Вызов агентов 🟢

Правил в `.cursor/rules/`: 28. Из них 16 системных и служебных, 12 руководителей плюс 3 алиаса по имени.

| Группа | Проверка |
|---|---|
| Службы | `@ceo`, `@сео`, `@иос`, `@архивариус`, `@аудитор`, `@архитектор` — файлы на месте |
| Бизнес | `@dept-стратегия` (+`@соломон`), `@dept-продажи`, `@dept-производство`, `@dept-продукт`, `@dept-финансы` |
| Личное | `@dept-ритм`, `@dept-дух`, `@dept-обучение`, `@dept-люди`, `@dept-семья`, `@dept-устойчивость`, `@dept-тело` (+`@анатолий`) |
| Ссылки внутри правил | 25 из 25 указывают на существующие файлы, битых нет |
| Триггеры по смыслу | у всех 12 в `description` рабочие формулировки владельца, не только `@имя` |
| `alwaysApply: true` | только `00-ядро`, `10-источники`, `11-верификация` — как задумано |

## Светофор 3 · Готовность к Модулю 3 🟡

| Критерий | Статус |
|---|---|
| Карта смыслов бизнеса | 🟢 v1.3, 95%, подтверждена владельцем |
| Карта смыслов личного | 🟡 v1.1, ~80%: не сверены цифры зала и не проверен импорт финансов |
| Реестр команды и общая база смыслов | 🟢 заполнены по факту |
| Системный слой (логи, дельты, скиллы) | 🟢 на месте, откат описан |
| Промпты руководителей | 🟡 60–70% по замыслу: остаток закрывают чек-листы первого дня |
| Конвейер входящего | 🟡 обкатан не был: ни один файл ещё не прошёл `новое/` → `_к_проверке/` → отдел |
| Пути модулей 3–10 | 🟢 таблица в `ВЕРСИЯ.md` заполнена, М3 идёт в `2. Бизнес/_код/` |

**Вывод: к Модулю 3 переходить можно.** Жёлтый цвет не блокирует: это не пробелы установки, а данные, которые появляются только от владельца.

---

## Что закрыть до Модуля 3 (в порядке важности)

1. **Прогнать один файл через конвейер.** В `_ВХОДЯЩЕЕ/новое/` лежат 15 непринятых объектов, включая транскрипты созвонов Берёза и Телемост. Без живого прогона неизвестно, работает ли раскладка.
2. **Чек-лист первого дня у трёх отделов:** ритм (корневая поломка), стратегия (три хвоста до сих пор без имён), финансы (август сведён только по 13-е).
3. **Сверить цифру зала** — единственная цифра в личной карте, которая держится на ощущении «стабильнее».

## Три главных пробела системы (не установки)

1. Три критичных хвоста подтверждены цифрой, но не названы по именам в `ХВОСТЫ.md`.
2. Маркетинг отложен до ~2026-09-12 при том, что единственный канал продаж — структурный риск номер два после долга.
3. Личный контур: дневной и недельный ритм не работает, а на нём стоят три требовательные нормы.

---

Следующая приёмка — после первого прогона конвейера или на еженедельном ретро.
