# Тест: предел грузомест WB на живом кабинете

Инструкция для исполняющей модели. Задача одна: подтвердить на живом API WB правило числа коробов и выкатить исправление ff-sync. Ничего сверх этого не трогать.

## Контекст

Сервис `ff-sync`: код в `2. БИЗНЕС/08_Системное/_код/ff-sync/`, прод на `root@72.56.8.43:/opt/ff-sync`, панель `http://72.56.8.43:8787`. Кабинет WB для теста: `cabinet_id = 3` (GripOn, ИП владельца, тестировать на нём разрешено). Живая поставка `WB-GI-276355488`: 2 задания (`5716719759`, `5717401785`), 1 короб `WB-MP-54470001`, в доставку не передана.

Что было. Код ставил предел коробов «заданий плюс один» по английской странице документации WB. На живом API поставка из 2 заданий приняла первый короб и отдала `409 FailedToAddSupplyTrbx` на второй.

Что нашли. Русская OpenAPI-спека WB `03-orders-fbs.yaml` между 02.09 и 04.09.2026 сменила описание метода `POST /api/v3/supplies/{supplyId}/trbx`:

- было: «Вы можете добавить столько же грузомест, сколько всего товаров в поставке, плюс ещё один»;
- стало: «В одном грузоместе может быть несколько заказов. Например, если в поставке 10 заказов, распределите их по коробам: система позволит создать не больше 5 грузомест. Для 20 заказов — не больше 10 грузомест, для 100 — не больше 50».

Английская страница на 10.09 не обновлена. Источник: зеркало спеки `github.com/eslazarev/wildberries-sdk/specs/03-orders-fbs.yaml`, коммиты `fa012995` (02.09, старый текст) и `73600ac3` (04.09, новый текст).

Гипотеза, которую проверяем: **предел = заданий // 2** (целочисленное деление). Код уже переписан под неё локально: `wb_supply.box_limit()`, `supply_flow.make_boxes()`, подсказка в `webui/app.js`, версия статики 56. На прод не выкачено.

## Правила безопасности

- Только кабинет 3 и поставки, созданные в ходе теста, плюс `WB-GI-276355488`. Чужие кабинеты не трогать.
- Не вызывать `PATCH .../deliver` ни на одной поставке. Шаг необратимый.
- Не переводить задания в поставку из числа тех, что уже лежат в других открытых поставках клиента.
- Каждый `4XX` у WB списывает 10 запросов из лимита 300 в минуту. Между запросами, которые ожидаемо дадут `409`, держать паузу не меньше 3 секунд, после серии из трёх `4XX` подождать 60 секунд.
- Созданные для теста короба удалять `DELETE .../trbx`, созданные пустые поставки удалять `DELETE /api/v3/supplies/{id}` (удаляется только пустая).

## Шаг 1. Снимок состояния (только чтение)

```bash
ssh -o BatchMode=yes root@72.56.8.43 "cd /opt/ff-sync && docker compose exec -T web python - <<'PY'
import requests
from db import get_cabinet, init_db
from net import WB_BASE, wb_headers
init_db(); cab = get_cabinet(3); h = wb_headers(cab['token'])
S = 'WB-GI-276355488'
for p in ('/api/v3/supplies/%s' % S, '/api/v3/supplies/%s/trbx' % S, '/api/marketplace/v3/supplies/%s/order-ids' % S):
    r = requests.get(WB_BASE + p, headers=h, timeout=30)
    print(p, r.status_code, (r.text or '')[:400])
PY"
```

Ожидание: `done: false`, `isPickupPointShipmentAllowed: true`, `cargoType: 1`, один короб, два задания. Если поставка уже закрыта или заданий не два, тест на ней не делать, перейти к шагу 3 с новой поставкой.

## Шаг 2. Проверка на поставке из двух заданий

Один короб уже есть. Запросить ещё один, зафиксировать код и тело ответа:

```bash
ssh -o BatchMode=yes root@72.56.8.43 "cd /opt/ff-sync && docker compose exec -T web python - <<'PY'
import requests
from db import get_cabinet, init_db
from net import WB_BASE, wb_headers
init_db(); cab = get_cabinet(3); h = wb_headers(cab['token'])
u = WB_BASE + '/api/v3/supplies/WB-GI-276355488/trbx'
r = requests.post(u, headers=h, json={'amount': 1}, timeout=30)
print('amount=1 ->', r.status_code, (r.text or '')[:300])
print({k: v for k, v in r.headers.items() if 'ratelimit' in k.lower()})
PY"
```

Ожидание по гипотезе: `409 FailedToAddSupplyTrbx`. Если пришёл `201`, гипотеза «половина» неверна: записать это, удалить лишний короб через `DELETE .../trbx` с телом `{"trbxIds": ["<id>"]}` и перейти к шагу 5, вариант Б.

## Шаг 3. Проверка на четырёх и одном задании

Нужны свободные задания WB кабинета 3 в статусе `new`, того же `cargoType: 1` (малогабарит) и того же склада, что и поставка. Взять их через панель: вкладка «Сборка», фильтр контрагента GripOn, столбец «Поставка» пустой. Либо из базы:

```bash
ssh -o BatchMode=yes root@72.56.8.43 "cd /opt/ff-sync && docker compose exec -T web python - <<'PY'
from db import init_db, connect
init_db()
rows = connect().execute(\"SELECT ext_id, status_group, cargo_type, office FROM shipments WHERE cabinet_id=3 AND marketplace='wb' AND kind='fbs' AND status_group='new' AND COALESCE(supply_ext,'')='' AND COALESCE(work_state,'')='' LIMIT 12\").fetchall()
for r in rows: print(dict(r))
PY"
```

Если имена колонок не совпадут, посмотреть `schema.sql`, таблица `shipments`, и поправить запрос. Понадобится 5 заданий: 4 для поставки А, 1 для поставки Б. Если свободных заданий меньше пяти, сделать только поставку А (или Б, если заданий меньше четырёх) и записать, какая часть не проверена.

Создать поставки и добавить задания. Добавление в поставку переводит задание в `confirm` («На сборке») и назад в `new` его не вернуть, но задания GripOn всё равно поедут в этой смене, так что это допустимо.

```bash
ssh -o BatchMode=yes root@72.56.8.43 "cd /opt/ff-sync && docker compose exec -T web python - <<'PY'
import time, requests
from db import get_cabinet, init_db
from net import WB_BASE, wb_headers
init_db(); cab = get_cabinet(3); h = wb_headers(cab['token'])
A_IDS = [111, 222, 333, 444]   # подставить 4 ext_id заданий
B_IDS = [555]                  # подставить 1 ext_id
def make(name, ids):
    r = requests.post(WB_BASE + '/api/v3/supplies', headers=h, json={'name': name}, timeout=30)
    sid = r.json()['id']; print('supply', name, r.status_code, sid)
    r = requests.patch(WB_BASE + '/api/marketplace/v3/supplies/%s/orders' % sid, headers=h, json={'orders': ids}, timeout=30)
    print('  add orders', r.status_code, (r.text or '')[:200])
    time.sleep(2)
    r = requests.get(WB_BASE + '/api/marketplace/v3/supplies/%s/order-ids' % sid, headers=h, timeout=30)
    print('  in supply:', r.text[:200])
    return sid
def boxes(sid, seq):
    u = WB_BASE + '/api/v3/supplies/%s/trbx' % sid
    for amount in seq:
        r = requests.post(u, headers=h, json={'amount': amount}, timeout=30)
        print('  amount=%s -> %s %s' % (amount, r.status_code, (r.text or '')[:200]))
        time.sleep(3)
    r = requests.get(u, headers=h, timeout=30); print('  boxes now:', r.text[:300])
A = make('Тест коробов А (4 задания)', A_IDS)
boxes(A, [1, 1, 1])   # ожидание: 201, 201, 409
B = make('Тест коробов Б (1 задание)', B_IDS)
boxes(B, [1])         # ожидание по гипотезе: 409; если 201 — предел max(1, n//2)
print('A =', A, 'B =', B)
PY"
```

Записать ровно: коды ответов по каждому запросу, тело `409`, итоговое число коробов в каждой поставке.

## Шаг 4. Уборка

Короба, созданные в шаге 3, можно оставить: они принадлежат поставкам GripOn, которые поедут в этой смене. Если решишь удалить лишние, `DELETE /api/v3/supplies/{id}/trbx` с телом `{"trbxIds": [...]}`. Поставки А и Б с заданиями внутри не удалять и в доставку не передавать. В панели они появятся в списке поставок контрагента после кнопки «Обновить» на вкладке «Сборка».

## Шаг 5. Решение и выкат

Вариант А, гипотеза подтверждена (2 задания — второй короб `409`; 4 задания — третий `409`; 1 задание — `409`). Код уже верный. Выкатить:

```bash
cd "/Users/max/CURSOR/2. БИЗНЕС/08_Системное/_код/ff-sync" && \
rsync -c README.md wb_supply.py supply_flow.py webui/app.js webui/index.html -e "ssh -o BatchMode=yes" root@72.56.8.43:/opt/ff-sync/ --relative && \
ssh -o BatchMode=yes root@72.56.8.43 'cd /opt/ff-sync && docker tag ff-sync ff-sync:base && docker build -q -f Dockerfile.patch -t ff-sync:latest . >/dev/null && docker compose up -d --force-recreate 2>&1 | tail -2 && sleep 6 && curl -s -m 10 localhost:8787/health && curl -s -m 10 localhost:8787/ | grep -o "app.js?v=[0-9]*"'
```

Если `--relative` положит файлы не туда (`/opt/ff-sync/webui/app.js` должен оказаться именно там), скопировать `webui/app.js` и `webui/index.html` отдельной командой `rsync -c webui/app.js webui/index.html -e "ssh -o BatchMode=yes" root@72.56.8.43:/opt/ff-sync/webui/`. Ожидание: `/health` отвечает `ok`, в HTML `app.js?v=56`.

Вариант А с поправкой: 1 задание дало `201`. Правило `max(1, orders // 2)`. Поправить одну строку в `wb_supply.py`:

```python
def box_limit(orders):
    return max(1, int(orders or 0) // TRBX_PER_BOX) if int(orders or 0) > 0 else 0
```

Затем в `_архив/test_supply.py` ничего менять не нужно (там проверки на 2, 3, 10 заданиях), прогнать `PYTHONPATH=. python3 _архив/test_supply.py`, ожидание последняя строка «все проверки поставок, сборки и КиЗ прошли». В README абзац «Поставка из одного задания короба не получает» переписать под факт. Выкатить как в варианте А.

Вариант Б: гипотеза опровергнута (второй короб на двух заданиях прошёл, или на четырёх заданиях прошли три). Ничего не выкатывать. Записать все коды и тела ответов в этот файл в раздел «Результат» и остановиться, решение за владельцем.

## Шаг 6. Проверка через панель

После выката открыть `http://72.56.8.43:8787`, вкладка «Сборка», контрагент GripOn, выбрать задания поставки `WB-GI-276355488`, нажать «Собрано». В окне вопроса о коробах должно стоять «максимум 1 при 2 заданиях (половина, округление вниз), уже создано 1». Ввести 1: ожидание, что отметка «Собрано» ставится, а в заметке текст «WB разрешает коробов не больше половины заданий: заданий 2, значит максимум 1, уже создано 1». Запроса в WB при этом уходить не должно.

## Что вернуть владельцу

Пять строк: результат шагов 2 и 3 кодами ответов, какой вариант шага 5 применён, вывод `/health` и версия `app.js`, скрин или текст заметки из шага 6. Дописать одну строку в `_СИСТЕМА/_ЛОГИ/посл_сессия.md` по формату `- ЧЧ:ММ · агент · что сделал · результат`.

## Результат

(заполняет исполняющая модель)
