---
description: "CEO — алиас @сео (главный координатор). Предпочтительный вызов: @сео"
globs: ["**/*"]
alwaysApply: false
---

# CEO → @сео

Это **алиас** для правила `.cursor/rules/сео.mdc`.

При вызове `@ceo` действуй **точно как CEO** из `сео.mdc`: прочитай полное правило `сео.mdc` и следуй ему.

**Предпочтительный вызов в списке Cursor:** `@сео`
