name: emmanuel-report-bot

services:
  bot:
    build: .
    container_name: emmanuel-report-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - emmanuel_report_bot_data:/data

volumes:
  emmanuel_report_bot_data:
