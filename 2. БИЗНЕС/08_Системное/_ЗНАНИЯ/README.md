# `_ЗНАНИЯ` — слой StartPack (SSOT)

Главный артефакт курса: [`КАРТА_СМЫСЛОВ.md`](КАРТА_СМЫСЛОВ.md).

Детальные файлы курса (`О_БИЗНЕСЕ`, `ПРОДУКТЫ`…) **не плодим**, пока карта не упрётся в объём: детали уже лежат в зонах `01_`–`07_` и в профиле. Карта — индекс + светофор пробелов.

Сырьё по умолчанию: [`_ВХОДЯЩЕЕ/`](../../../0.%20Входящее/) (якорь inbox vault). Отдельный `_ИСХОДНИКИ/` — только если понадобится для ДЗ/ритуала.
