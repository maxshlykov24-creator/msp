# AI-Менеджер 1-й линии · OpenClaw workspace

> Карточка первого AI-сотрудника курса. Реализовано на **OpenClaw 2026.5+** — тот же фреймворк, что у Дениса в production (`@denis_pleada_bot`). Шаблон, на котором собираются все следующие сотрудники из М4–М8.

## Кто это

OpenClaw-агент, который через Telegram квалифицирует входящие лиды и передаёт квалифицированный лид живому менеджеру с резюме диалога. Работает 24/7 на VPS под одним gateway-процессом. Говорит голосом твоей компании из КОНТЕКСТ.md (М2). Опционально читает базу знаний компании и пишет резюме в выходную папку для систематизации.

## Что меняется vs «классический» бот на коде

| Раньше | OpenClaw |
|---|---|
| Писал Python-код бота | Пишешь markdown-конфиг, агент Cursor его собирает за тебя |
| Один процесс = один бот | Один gateway → много агентов под разные роли |
| Каждый бот свой systemd-юнит | Один systemd-юнит, в нём все агенты |
| Multi-канал = свой адаптер на канал | TG / Discord / iMessage / Slack из коробки |
| Tools захардкожены в коде | Tool registry — bash/CLI скрипты, агент сам решает что вызвать |
| Cron — отдельный systemd timer | Встроен в OpenClaw |
| Память диалога — in-memory | Persistent через memory module |

**Главное:** ты не пишешь код. Cursor пишет markdown-конфиг под твою роль из артефактов М2.

## Состав папки

| Файл | Что |
|---|---|
| README.md | этот файл |
| **ПРОМПТ_СБОРКИ.md** | команда агенту Cursor: собрать workspace из артефактов М2 (5 markdown-файлов) |
| **ПРОМПТ_ДЕПЛОЯ.md** | команда агенту Cursor: развернуть OpenClaw + lead-qualifier + Telegram + systemd на VPS |
| .env.example | переменные окружения (OPENROUTER_API_KEY, TG_BOT_TOKEN, MANAGER_CHAT_ID) |
| openclaw.config.example.json | шаблон агента в `openclaw.json` |
| workspace/AGENTS.md | главный промпт роли (правила + сценарий + критерии) |
| workspace/IDENTITY.md | кто, голосом кого, тон |
| workspace/BOOTSTRAP.md | стартовая инструкция: что делать при первом сообщении |
| workspace/HEARTBEAT.md | что делать на heartbeat (по умолчанию пусто — реактивный режим) |
| workspace/MODE_LEAD_QUAL.md | детали режима квалификации лидов (5–7 вопросов, формат summary) |
| workspace/_ВЫХОД/ | сюда агент пишет резюме каждого квалифицированного лида |
| tools/escalate.sh | передача живому менеджеру (push в TG-чат) |
| tools/save_lead.sh | сохранить резюме в _ВЫХОД/ |
| tools/kb_search.sh | поиск по базе знаний (опц.) |

## Запуск · через два промпта Cursor

Ты не вводишь команды руками. Ты пишешь два промпта в чате Cursor.

```
1. ПРОМПТ_СБОРКИ.md → Cursor (локально)
   ↓ Cursor читает КОНТЕКСТ.md, ВОРОНКА.md, МАТРИЦА_РОЛЕЙ.md из М2
   ↓ заполняет 5 markdown-файлов workspace под твой бизнес
   = workspace готов

2. ПРОМПТ_ДЕПЛОЯ.md → Cursor (на VPS через Remote-SSH)
   ↓ ставит Node.js 20+
   ↓ npm install -g openclaw
   ↓ openclaw configure
   ↓ openclaw agents create lead-qualifier
   ↓ openclaw channels add telegram
   ↓ openclaw gateway install --systemd
   = бот работает 24/7 на твоём VPS
```

Между ними — `rsync` workspace на VPS и `nano .env` (одна правка переменных). Подробно — `_ПОДДЕРЖКА/ДЕПЛОЙ_VPS.md`.

## Что менять и где

| Хочу изменить | Файл |
|---|---|
| Как бот говорит, какие вопросы задаёт | `workspace/AGENTS.md` или `workspace/MODE_LEAD_QUAL.md` |
| Голос, тон, обращение | `workspace/IDENTITY.md` |
| Что бот делает при первом сообщении | `workspace/BOOTSTRAP.md` |
| Какие tools доступны | `openclaw.config.example.json` → `tools.profile` + `tools/` папка |
| Модель LLM | `openclaw.config.example.json` → `model` |
| Чат менеджера (куда передавать лидов) | `.env` → `MANAGER_CHAT_ID` |

## KPI (для последующих модулей)

- **% квалифицированных от входящих** — целевой 30–60% (зависит от качества трафика).
- **Время до квалификации** — 5–10 сообщений / 3–7 минут.
- **Доля «передан без ответа»** — должна снижаться (ловится в М6 ОКК).

## Отложено: создание сделки в amoCRM по номеру

Решено 9 сентября 2026, к работе не приступали. Сейчас бот в CRM не пишет вообще: телефон уходит в файл лида, `tools/amo_client.py` работает только на чтение.

Что уже согласовано, чтобы не переспрашивать при возврате к задаче:

- **Триггер** — клиент прислал телефон в диалоге.
- **Воронка** «Продажи» `10372290`, **статус «Новая заявка»** `82003646` (не «Неразобранное»), ответственный Никита `13334858`.
- **Источник** (поле `2026903`) — существующие значения по каналу обращения: «Авито чат», «Авто.ру чат». Новое значение под ИИ-агента не заводим.
- Контакт ищем по номеру, нет — создаём. Машина из диалога идёт в поля VIN, марка, модель, год (`2027421`–`2027435`), переписка — в примечание к сделке.
- Вторую сделку по тому же Telegram chat_id не создаём: `amo_lead_id` хранится в состоянии диалога.
- Долгосрочный токен лежит в `DIVO_Motors/06_Доступы/.env`, на VPS бота его нет — перенести в `.env` перед стартом.
- Порядок запуска: сначала одна тестовая сделка со ссылкой владельцу, включение — после подтверждения.

Справочник воронок, статусов и полей — `2. БИЗНЕС/04_Производство/Активные/DIVO_Motors/02_Знание/КОНТЕКСТ_AMOCRM.md`.

## Дальше

После того, как этот сотрудник заработал — открывается **Модуль 4: Транскрибатор**. Тот же gateway, новый агент — `openclaw agents create transcriber`. К Модулю 8 у тебя 5 рабочих агентов под одним gateway. Конфиги — в [`ВАРИАЦИИ_СОТРУДНИКОВ.md`](../../../ВАРИАЦИИ_СОТРУДНИКОВ.md).
