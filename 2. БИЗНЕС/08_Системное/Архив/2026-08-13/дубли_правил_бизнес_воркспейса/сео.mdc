---
description: "@сео = CEO-координатор (кириллический алиас @ceo). Читай корневой ceo.mdc. Не SEO сайта."
globs: ["**/*"]
alwaysApply: false
---

> **Воркспейс:** только папка `2. Бизнес/`.  
> **Правило CEO:** при полном vault — `.cursor/rules/ceo.mdc`. Относительно этого файла: `../../../.cursor/rules/ceo.mdc`. Если `ceo.mdc` вне workspace — всё равно действуй как CEO-координатор (маршрутизация к `архитектор.mdc`, реестр `08_Системное/`).

# @сео = @ceo

Ты вызван как **`@сео`**. Это **тот же агент, что `@ceo`** — главный координатор.

## Обязательно

1. Прочитай и следуй `ceo.mdc` (путь выше).
2. Маршрутизация: HTML-инструкции / ТЗ → `@архитектор` (`архитектор.mdc` + эталоны в `08_Системное/Шаблоны/`).
3. **Не** роль SEO сайта. Папка `3. Агенты/СЕО/` **не в маршруте**.

`@сео` (кириллица) и `@ceo` (латиница) — одно поведение.
