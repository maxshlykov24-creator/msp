# 06_Доступы

Секреты клиента. Файл `ДОСТУПЫ.md` в git не попадает (шаблон `**/ДОСТУПЫ*.md`).

В контекст чата — только по явной просьбе. Ключи и пароли не повторять в ответах.

Наверх: [`../README.md`](../README.md)
