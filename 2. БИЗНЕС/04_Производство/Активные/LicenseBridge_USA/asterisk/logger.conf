;
; Логи Asterisk. До 2026-07-26 файла не было вообще: ни один звонок не попадал в
; /var/log/asterisk, и разбирать инциденты было нечем.
;
[general]
dateformat=%F %T

[logfiles]
console => notice,warning,error
messages => notice,warning,error
full => notice,warning,error,verbose(3),dtmf
