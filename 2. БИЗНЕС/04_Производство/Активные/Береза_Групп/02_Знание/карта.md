# Интеграционная карта — RUSbrend (Вариант 1)

**Клиент:** rusbrend
**Дата:** 2026-05-14
**Scope:** Google Sheets ↔ МойСклад через Google Apps Script

**Легенда цветов:** 🟢 Google Sheets · 🟠 Apps Script · 🔵 МойСклад · 🟣 Пользователи · 🔴 Ошибки / расхождения · 🟤 Заказ · 🌿 Приёмка

**Статусы отгрузки (как в МойСклад):** ⚪ Новая · 🔵 В сборке · 🟠 На проверке · 🟡 Проверено · 🟢 Отправлена · 🔴 Отменена

**Инструкция для Серёжи:** [`../05_Материалы/инструкция/Серёжа_МойСклад.html`](../05_Материалы/инструкция/Серёжа_МойСклад.html)

---

## Диаграмма: поток данных

```mermaid
flowchart TD
    subgraph sheets ["Google Sheets — Склад Белград"]
        A["Лист «План работы»<br/>Артикул · Итого коробов (H) · Магазин"]
        B["Флажок «Отгрузить» (I)<br/>Управляющий"]
        C["Столбец «ID в МойСклад» (J)<br/>Заполняет скрипт"]
    end

    subgraph gas ["Google Apps Script"]
        D["onChange-триггер<br/>на флажок"]
        E["Читает строку:<br/>артикул, коробов, магазин"]
        F["POST /entity/demand<br/>× N коробов"]
        G["Записывает ID отгрузок<br/>в столбец J"]
        ERR["Ошибка API → лист Errors<br/>+ Telegram-алерт"]
    end

    subgraph ms ["МойСклад"]
        H["Номенклатура<br/>~96 SKU · атрибут Магазин"]
        I["Отгрузка<br/>1 короб = 1 SKU<br/>статусы см. ниже"]
        J["Приёмка<br/>по заказу поставщику"]
        K["Заказ поставщику"]
        L["Остатки · склад Белград"]
        M["Склад Фулфилмент<br/>клиентский товар"]
    end

    subgraph users ["Пользователи"]
        LEAD["Руководитель<br/>заказ поставщику<br/>остатки"]
        MGR["Управляющий<br/>флаг · приёмка<br/>статусы · ШК"]
        WORKERS["Сборщики<br/>статус «В сборке»"]
    end

    B -->|onChange| D
    D --> E --> F
    F -->|создаёт| I
    F -->|ошибка| ERR
    I --> G --> C

    LEAD -->|создаёт| K
    K -->|основание| J
    J -->|обновляет| L
    I -->|списывает| L

    MGR -->|ставит флаг| B
    MGR -->|закрывает приёмку| J
    MGR -->|статус · ШК| I
    WORKERS -->|сборка| I

    H -->|справочник| I
    H -->|справочник| J
    M -.->|отдельный склад| L

    classDef sheetsNode fill:#f4f8f4,stroke:#b8cdb8,color:#4a5d4a,stroke-width:1px
    classDef gasNode fill:#faf6f0,stroke:#dccfb8,color:#6b5e4a,stroke-width:1px
    classDef msNode fill:#f3f7fb,stroke:#b8cfe0,color:#4a5f6b,stroke-width:1px
    classDef userNode fill:#f8f4fa,stroke:#cfc0dc,color:#5a4a6b,stroke-width:1px
    classDef errNode fill:#fdf4f4,stroke:#ddb8b8,color:#6b4a4a,stroke-width:1px

    class A,B,C sheetsNode
    class D,E,F,G gasNode
    class H,I,J,K,L,M msNode
    class LEAD,MGR,WORKERS userNode
    class ERR errNode

    style sheets fill:#f8fbf8,stroke:#c8dcc8,stroke-width:1px
    style gas fill:#fcfaf6,stroke:#e8dcc8,stroke-width:1px
    style ms fill:#f6f9fc,stroke:#c8d8e8,stroke-width:1px
    style users fill:#faf8fc,stroke:#dcd4e8,stroke-width:1px
```

---

## Диаграмма: жизненный цикл отгрузки

```mermaid
flowchart TD
    START(( )) --> S_NEW["Новая"]
    S_NEW -.- NOTE["флаг в Sheets<br/>скрипт создаёт отгрузку"]
    S_NEW -->|"назначен ответственный"| S_PICK["В сборке"]
    S_PICK -->|"короб собран"| S_CHECK["На проверке"]
    S_CHECK -->|"проверка OK"| S_VER["Проверено"]
    S_CHECK -->|"ошибка"| S_PICK
    S_VER -->|"подтверждение"| S_SHIP["Отправлена"]
    S_SHIP --> FINISH(( ))

    S_NEW -.->|"отмена"| S_CANC["Отменена"]
    S_PICK -.->|"ошибка или пересмотр"| S_CANC

    classDef stNew fill:#f5f5f4,stroke:#c8c8c6,color:#57534e,stroke-width:1px
    classDef stPick fill:#eef6fb,stroke:#9ec5dc,color:#475569,stroke-width:1px
    classDef stCheck fill:#fdf4eb,stroke:#e8c4a0,color:#78716c,stroke-width:1px
    classDef stVerified fill:#fdfae8,stroke:#e8d898,color:#854d0e,stroke-width:1px
    classDef stShipped fill:#f0f7f2,stroke:#a8cfc0,color:#4a5d52,stroke-width:1px
    classDef stCancel fill:#fdf2f2,stroke:#dcb4b4,color:#7f4a4a,stroke-width:1px
    classDef stMeta fill:#fafaf9,stroke:#d6d3d1,color:#78716c,stroke-dasharray:4 4

    class S_NEW stNew
    class S_PICK stPick
    class S_CHECK stCheck
    class S_VER stVerified
    class S_SHIP stShipped
    class S_CANC stCancel
    class NOTE stMeta
    style START fill:#ffffff,stroke:#d6d3d1,color:#57534e
    style FINISH fill:#ffffff,stroke:#d6d3d1,color:#57534e
```

---

## Диаграмма: процесс приёмки

```mermaid
flowchart TD
    subgraph p1 ["Фаза 1 · Заказ"]
        R1["Руководитель"]
        P1["Поставщик"]
        MS1["МойСклад"]
        R1 -->|"план: артикул, кол-во"| MS1
        P1 -->|"привозит товар"| MS1
    end

    subgraph p2 ["Фаза 2 · Приёмка"]
        M2["Управляющий"]
        MS2["МойСклад"]
        M2 -->|"пересчёт"| M2
        M2 -->|"открывает приёмку"| MS2
        M2 -->|"фактическое кол-во"| MS2
        MS2 -->|"остатки"| MS2
    end

    subgraph p3 ["Фаза 3 · Расхождение"]
        MS3["МойСклад"]
        M3["Управляющий"]
        R3["Руководитель"]
        P3["Поставщик"]
        MS3 -->|"дельта"| M3
        M3 -->|"сообщает"| R3
        R3 -->|"разбор"| P3
    end

    p1 --> p2
    p2 -.->|"расхождение"| p3

    classDef actorUser fill:#f8f4fa,stroke:#cfc0dc,color:#5a4a6b,stroke-width:1px
    classDef actorMs fill:#f3f7fb,stroke:#b8cfe0,color:#4a5f6b,stroke-width:1px
    classDef actorSup fill:#faf6f0,stroke:#dccfb8,color:#6b5e4a,stroke-width:1px

    class R1,R3,M2,M3 actorUser
    class MS1,MS2,MS3 actorMs
    class P1,P3 actorSup

    style p1 fill:#faf6f0,stroke:#dccfb8,stroke-width:1px
    style p2 fill:#f4f8f4,stroke:#b8cdb8,stroke-width:1px
    style p3 fill:#fdf4f4,stroke:#ddb8b8,stroke-width:1px
```

---

## Компоненты и роли

| Компонент | Роль в системе | Кто управляет |
|-----------|----------------|---------------|
| Google Sheets "Склад Белград" / "План работы" | Источник задач для создания отгрузок | Управляющий |
| Google Apps Script | Middleware: триггер + вызов МойСклад API | Максим (настройка) |
| МойСклад — Номенклатура | Справочник товаров, SKU, баркоды, атрибуты | Максим (импорт) / Руководитель (поддержка) |
| МойСклад — Отгрузка | Учёт отгрузок, задачи сборщикам, статусы | Управляющий |
| МойСклад — Приёмка | Учёт приёмки товара, остатки | Управляющий |
| МойСклад — Заказ поставщику | Плановая закупка | Руководитель |
| Склад "Белград" | Основной склад RUSbrend + Gripon | Управляющий |
| Склад "Фулфилмент" | Клиентский товар | Управляющий |
| Шаблоны печати | Этикетка на товар и на короб | Максим (настройка) |

**Цвета статусов отгрузки (МойСклад):**

| Статус | Цвет | Кто переводит |
|--------|------|---------------|
| Новая | серый | скрипт при создании |
| В сборке | синий | управляющий назначает ответственного |
| На проверке | оранжевый | сборщик завершил короб |
| Проверено | жёлтый | управляющий после проверки |
| Отправлена | зелёный | управляющий перед погрузкой |
| Отменена | красный | ручная отмена |

---

## За пределами интеграции (Вариант 1)

- Ozon ЛК — создание поставок на маркетплейс (остаётся как раньше)
- ЗП-таблица "Склад Белград" — KPI и расчёт зарплат сборщиков
- WB / Яндекс ЛК — пока не подключены
- Сканирование коробов перед отгрузкой — Вариант 2
- ШК короба для Ozon — берётся из Ozon ЛК; внутренняя этикетка МойСклад — основа для Варианта 2
