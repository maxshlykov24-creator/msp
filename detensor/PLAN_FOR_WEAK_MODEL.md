# 🤖 ПЛАН ДЛЯ СЛАБОЙ МОДЕЛИ — DETENSOR / МойСклад

## Контекст (прочитай первым делом)

Это репозиторий с готовыми Python-скриптами, которые работают через REST API МойСклад.

Что уже сделано (твой коллега-модель сделал в предыдущей сессии):
- ✅ Шаг 2: Исправлено 88 артикулов
- ✅ Шаг 3: Переименовано 87 товаров
- ✅ Шаги 4–7: Заполнены Жёсткость/Длина/Категория/Линейка для 99 «целевых» SKU
- ✅ Шаг 8: 24 старые карточки «Устройство мед. DETENSOR» удалены вручную владельцем

Что осталось:
- Дозаполнить категории/линейки/жёсткость для ~29 модулей и сопутствующих товаров (**Шаг 11**)
- Шаг 12 (архивация мусора) — **no-op**: 62 мусорных товара находятся в папке «Товары интернет-магазинов» и не трогаются
- Финальная сверка через верификатор/аудит

---

## ⚠️ Правила работы

1. **Ничего не выдумывай!** Все ID, артикулы, токены, маппинги уже в файлах.
2. **Всегда сначала `--dry-run`**, потом боевой запуск. Только так.
3. **Не редактируй классификаторы и маппинги** в `step11_*` и `step12_*` — там всё уже выверено.
4. **Не запускай шаги 2–7 повторно** — они уже выполнены.
5. **После каждого боевого шага** запусти `audit.py` и убедись, что нет регрессий.
6. **Токен МойСклад** уже в `config.py` (`MS_TOKEN`). Менять не нужно.
7. **Лимит API**: 4 req/s (защита в `ms_client.py`). ~29 товаров обновятся ~8 секунд.
8. Если шаг падает на одном товаре — **не паникуй**, посмотри лог, исправь именно тот товар, продолжай.

---

## 🚦 Алгоритм (выполняй по порядку)

### Действие 1 — Перейти в директорию и проверить, что окружение готово

```bash
cd /workspace/detensor
python3 -c "from ms_client import get_all_products; print('OK:', len(get_all_products()), 'товаров')"
```

Ожидаемый вывод: `OK: 291 товаров` (число может слегка отличаться, если владелец что-то добавлял).

Если выходит ошибка `ModuleNotFoundError: requests` — установи:
```bash
pip install requests
```

---

### Действие 2 — Шаг 11 (дозаполнение): сначала DRY-RUN

```bash
python3 step11_complete_categories.py --dry-run
```

В выводе должно быть:
- `Пропущено (Сырье/ТИМ): ~167`
- `К обновлению: ~29 товаров в 6 группах`
- Группы G1, G2, G3, G12, G14, G15 — по 2–13 товаров
- В конце: `Итог: OK=~29, ERR=0`

**Если число товаров отличается на ±5** — это нормально (владелец мог что-то менять руками).
**Если ERR > 0 или вывод сильно отличается** — НЕ запускай боевой, остановись и доложи владельцу.

---

### Действие 3 — Шаг 11 БОЕВОЙ

```bash
python3 step11_complete_categories.py
```

Ожидаемый вывод: тот же список, но с `OK` вместо `[DRY]`, и финал: `Итог: OK=~29, ERR=0`.

**Если есть ERR** — посмотри какие товары падали, запиши их `id` и `name`, доложи владельцу.

---

### Действие 4 — Проверка после Шага 11

```bash
python3 verify.py
```

Ожидаемый вывод:
```
Целевых SKU найдено в каталоге: 89 из 89
Проблем с заполнением: 0
✅ Все целевые поля заполнены корректно
```

Это проверка по 89 «целевым» SKU из исходного ТЗ — она должна остаться зелёной.

Дополнительно запусти аудит:
```bash
python3 audit.py
```

Смотри секцию «1. ПОКРЫТИЕ ШАГА 6». Должно быть:
- `Исключено (Сырье/ТИМ): ~167 — не проверяем`
- `В области проверки: ~124`
- `В области проверки с Категорией: ~118` (было 89 + 29 от Шага 11)
- `В области проверки без Категории: ~6` (серая зона G17 — упаковка/инструкции)

---

### Действие 5 — Шаг 12 (архивация мусора): DRY-RUN для проверки

```bash
python3 step12_archive_trash.py --dry-run
```

Ожидаемый вывод:
```
К архивации: 0
Пропущено (Сырье/ТИМ): 62 — мусор в исключённых папках, не трогаем по решению владельца
Скрипт no-op: ...
```

Это нормально. 62 мусорных товара находятся в «Товарах интернет-магазинов» — их не трогаем.
**Боевой запуск Шага 12 не нужен.**

---

### Действие 6 — Финальная сверка

```bash
python3 verify.py && python3 audit.py
```

Что должно быть в `audit.py`:
- `Активных: 291`
- `Исключено (Сырье/ТИМ): ~167 — не проверяем`
- `В области проверки: ~124`
- `В области проверки с Категорией: ~118`
- `В области проверки без Категории: ~6` (G17 «упаковка/инструкции» — серая зона, см. ниже)

---

## 🚫 Что мы намеренно не трогаем

Корневые папки **`Сырье`** (~92 товара) и **`Товары интернет-магазинов`** (~75 товаров) **полностью исключены из обработки** по решению владельца.

Это полуфабрикаты (материалы/компоненты) и карточки для сайтовых интеграций — они синхронизируются отдельно.

Если видишь товары из этих папок в `audit.py` — **это норма, не считай это проблемой** и не пытайся их категоризировать или архивировать. Скрипты `step11` и `step12` их автоматически пропускают.

---

## 🟡 СЕРАЯ ЗОНА (не трогай — нужно решение владельца)

После Шагов 11 и 12 в каталоге останутся ~25 товаров без категории — это **группа G17 «упаковочное и инструкции»**:

| Товар | Возможное решение |
|---|---|
| «Инструкция Детензор 18%», «Инструкция для ночной подушки», «Инструкция подушка для сидения/спины» | Архив (сами по себе не продаются) или категория «Сопутствующие» |
| «Коробка для валиков», «Коробка для ночной подушки», «Коробка подушка для сидения/спины», «Коробка Люмбус» | Архив (упаковочный материал) |
| «Сумка Детензор» | Сопутствующие или Аксессуары к мату |
| «Стропа карабин для крепежа матрасов Fibratop», «Стропа для подспинного элемента» | Аксессуары к мату/матрасам |
| «Мешок п/э 1100×2250», «Мешок 400×750» — 5 шт | Архив (упаковка) |
| «Полимер подушка на сиденье», «Полимер подушка под спину» | Архив (сырьё, не товар) |

**Тебе как слабой модели — не принимай это решение!** Покажи этот список владельцу и спроси, что с ними делать.

---

## 📦 Что лежит в репозитории

| Файл | Назначение | Запускать? |
|---|---|---|
| `config.py` | Все ID атрибутов, справочников и значений | Нет |
| `ms_client.py` | HTTP-клиент с retry/rate-limit | Нет |
| `step2_fix_articles.py` | Шаг 2 — артикулы | **Не нужно**, уже выполнено |
| `step3_rename.py` | Шаг 3 — переименование | **Не нужно**, уже выполнено |
| `step4567_fill_attrs.py` | Шаги 4–7 — атрибуты | **Не нужно**, уже выполнено |
| `step11_complete_categories.py` | Шаг 11 — дозаполнение модулей/чехлов | **Запускай** (dry-run → боевой) |
| `step12_archive_trash.py` | Шаг 12 — архивация мусора | **Запускай** (dry-run → боевой) |
| `verify.py` | Верификатор по 89 целевым SKU | Запускай после каждого шага |
| `audit.py` | Глубокий аудит всего каталога | Запускай после каждого шага |
| `check_after_user.py` | Аудит «что осталось без категории» | Опционально, для отладки |
| `README.md` | Документация | Для чтения |

---

## 🔍 Подводные камни (которые мы уже наступили)

1. **GET-запрос** требует заголовок `Accept: application/json;charset=utf-8` — уже зашит в `ms_client.py`. Не трогай.
2. **`customentity` поле** обновляется объектом `{meta: {href: ...}}`, не строкой. Помощник `make_attr()` в `ms_client.py` всё делает сам.
3. **Поле «Жесткость» без ё** — в API оно так и называется (без ё). В `config.py` тоже без ё. Если будешь редактировать — пиши без ё.
4. **Артикул в МойСклад не уникален** — у двух товаров может быть одинаковый article. Это OK, не считай это ошибкой.
5. **Чёрные/чёрный**: в названиях встречаются и `Чёрный`, и `Черный`. Регекспы в скриптах используют lowercase и обрабатывают оба варианта.
6. **Аренда** (артикулы `rent28_*`) — в каталоге МойСклад **отсутствует**. По ТЗ должно быть 12 SKU, но их нет — это вне твоей задачи.

---

## 🆘 Что делать, если что-то идёт не так

| Симптом | Действие |
|---|---|
| `RuntimeError: ... → 401` | Токен в `config.py` отозван. Доложи владельцу — нужен новый. |
| `RuntimeError: ... → 403` | Прав не хватает. Доложи владельцу. |
| `RuntimeError: ... → 429` | Превышен rate-limit. Скрипт сам ждёт 2с и ретраит — просто подожди. |
| `RuntimeError: ... → 5xx` | Сервер МойСклад. Скрипт ретраит с backoff. Если всё равно — попробуй через 5 минут. |
| `Итог: ERR > 0` после dry-run | Скорее всего ошибка в классификаторе. **Не запускай боевой**. Доложи владельцу с логом. |
| Количество товаров ±20% от ожидаемого | Каталог поменялся. Сравни с разделом «Текущие группы» в `check_after_user.py` (запусти его). |

---

## ✅ Definition of Done (когда задача считается выполненной)

1. `step11_complete_categories.py` выполнен боевым, `OK ≈ 29, ERR=0`
2. `step12_archive_trash.py` — no-op (запускать не обязательно)
3. `verify.py` показывает `89/89, проблем: 0`
4. `audit.py` показывает «В области проверки с Категорией ≈ 118» и «В области проверки без Категории ≈ 6»
5. Список из ~6 оставшихся без категории (G17 «серая зона») передан владельцу для ручного решения

Когда всё это сделано — отчитайся владельцу одним сообщением со ссылкой на этот документ и финальными цифрами.
