# AGENTS.md

## Cursor Cloud specific instructions

This repository (`msp`) is currently an empty/freshly-initialized project with only a `README.md`. There are no services, dependencies, build systems, or application code yet.

### Current state
- No tech stack has been chosen yet.
- No dependency files (`package.json`, `requirements.txt`, `pyproject.toml`, `go.mod`, etc.) exist.
- No CI/CD, Dockerfiles, or dev container configurations are present.
- No lint, test, or build commands are available.

### When code is added
Once the codebase gains application code, this section should be updated with:
- How to install dependencies (update script)
- How to run lint/test/build
- How to start dev services
- Any non-obvious caveats discovered during setup
