# Дельты 2026-09-10

## #01 · 13:05 · Ио

**Действие:** ff-sync, предел грузомест WB переведён с «заданий плюс один» на «половина заданий, округление вниз». Причина: русская OpenAPI-спека WB сменила правило между 02.09 и 04.09.2026, английская страница не обновлена; живой API отдал `409 FailedToAddSupplyTrbx` на втором коробе поставки из двух заданий. Первый короб больше не блокируется нашей проверкой: когда расчёт даёт ноль (одно задание), решение отдаётся площадке.

**Файлы:**
- `2. БИЗНЕС/08_Системное/_код/ff-sync/wb_supply.py` (`TRBX_EXTRA` → `TRBX_PER_BOX` и `box_limit()`)
- `2. БИЗНЕС/08_Системное/_код/ff-sync/supply_flow.py` (`make_boxes`)
- `2. БИЗНЕС/08_Системное/_код/ff-sync/webui/app.js`, `webui/index.html` (подсказка, версия статики 56)
- `2. БИЗНЕС/08_Системное/_код/ff-sync/README.md`
- `2. БИЗНЕС/08_Системное/_код/ff-sync/_архив/test_supply.py`
- `_ВХОДЯЩЕЕ/новое/2026-09-10_Тест_коробов_WB.md` (инструкция и отчёт о прогоне)

**Откат:** вернуть эти файлы из git на состояние до записи, версию статики на 55, залить на прод `rsync` и пересобрать `Dockerfile.patch`. Предел снова станет «заданий плюс один», и второй короб на поставке из двух заданий снова будет уходить в WB и получать `409`.

## #02 · 13:12 · Ио

**Действие:** кабинет WB ООО «ДЕЛЬТА» (`cabinets.id = 4`) снят с выгрузки по решению владельца. В нём был записан тот же токен, что у кабинета GripOn (`cabinets.id = 3`): совпадали хеш и длина, из-за чего «Дельта» показывала заказы и каталог GripOn — 48 заданий и 252 позиции. Флаг `active` перезаписывается при «Обновить клиентов», пока токен валиден, поэтому снят и токен.

**Что изменилось в базе прода** (`/opt/ff-sync`, таблица `cabinets`, строка `id = 4`): `token` очищен, `active` = 0, `last_ok_at` = NULL, `last_error` = «токен совпадал с кабинетом GripOn (ИП Шлыков) — снят 10.09.2026 до получения своего от клиента».

**Откат:** токен на диск не пишем. До получения токена от «Дельты» откат — скопировать значение из кабинета 3 (`UPDATE cabinets SET token = (SELECT token FROM cabinets WHERE id = 3), active = 1, last_error = '' WHERE id = 4`), но это вернёт и подмену данных. Правильный путь — вписать свой токен клиента через карточку в МойСклад и нажать «Обновить клиентов».

**Дальше:** чужие задания и каталог удалены в дельте #03.

## #03 · 13:26 · Ио

**Действие:** по решению владельца удалены чужие данные WB, натянутые под ООО «ДЕЛЬТА» из кабинета GripOn. Сверено до удаления: 48 из 48 заданий и 252 из 252 позиций каталога совпадали с кабинетом 3. Счетов у клиента не было. Партии склада (8 лотов напильников и скоб от 28.08, 10 движений, 8 строк приёмки) свои — не трогал. Карточка клиента и сам кабинет 4 (без токена, `active = 0`) остались.

**Что удалено в базе прода** (`cabinets.id = 4`): 48 `shipments`, 252 `catalog_cache`, 8 `sku_map`, 46 `orders_log`, 0 `wb_supplies`, 0 `shipment_marks`.

**Копия для отката:** `/root/ff.db.bak-2026-09-10-delta-purge` на VPS (26 МБ, снята до удаления). В vault не копировать: внутри токены.

**Откат:** на VPS `cp /root/ff.db.bak-2026-09-10-delta-purge /var/lib/docker/volumes/ff-sync_ff_data/_data/ff.db` и перезапуск `docker compose up -d` в `/opt/ff-sync`. Это вернёт и токен GripOn в кабинет 4 — после отката снова снять токен командой из дельты #02.

## #04 · 15:12 · Ио

**Действие:** по решению владельца карточки контрагентов в МойСклад приведены к именам и реквизитам, которые отдал WB `seller-info` по двум токенам. Источник юр.реквизитов ООО — [Rusprofile](https://www.rusprofile.ru/id/1257700239180) на 10.09.2026.

| Было | Стало | ИНН |
|---|---|---|
| ИП Вахрушев Глеб Максимович (`f0962e48-…204df1`) | ООО «ЛЕГКО ИДЁТ», бренд JOYCITY | 9701312653 |
| ИП Шлыков Максим Александрович (`7cfe8dab-…26dd5f`) | ИП Вахрушев Глеб Максимович, бренд GripOn | 665813832111 |

ООО «ДЕЛЬТА» не трогал. В ff-sync обновлены только отображаемые имена клиентов и кабинетов WB, коды `TEST` / `IPSHLYKOVMAK` и токены на месте.

**Откат:** вернуть поля `name`, `legalTitle`, `companyType`, `inn`, `kpp`, `ogrn`, `ogrnip`, `okpo`, `legalAddress`, ФИО в тех же карточках МойСклад на значения до правки (Вахрушев на первой, Шлыков на второй).

## #05 · 15:35 · Ио

**Действие:** перед панелью ff-sync поставлен nginx на 80 и 443. Браузер склада сам уходит на HTTPS, а запрос на голый IP `https://72.56.8.43/` снаружи зависает (Timeweb/путь до 443 без SNI). По имени `https://72.56.8.43.sslip.io/` TLS отвечает. Сертификат пока свой: Let's Encrypt не достучался до порта 80 с своих адресов.

**Файлы:** `docker-compose.yml`, `edge/nginx.conf`, `README.md`. Сертификат лежит на VPS в `/opt/ff-sync/edge/certs/`, в git не входит.

**Откат:** в `docker-compose.yml` убрать сервис `edge`, вернуть у `web` проброс `80:8787`, на проде `docker compose up -d --force-recreate web` и `docker compose stop edge`.

## #06 · 16:28 · Ио

**Действие:** панель ff-sync перенесена на VPS `194.87.202.217` (`ams-1-vm-bbl3`). Код, `.env` и база `ff.db` скопированы со старого `72.56.8.43`. Старый сервер не гасил. HTTPS на голый новый IP снаружи отвечает (на старом зависал).

**Откат:** склад снова на `https://72.56.8.43.sslip.io/`. На новом: `cd /opt/ff-sync && docker compose down`.

## #07 · 16:58 · Ио

**Действие:** колонка «Куда везти» у WB больше не показывает кластер покупателя. Малогабарит — ПВЗ Домодедовская, 28; крупный или отказ площадки — СЦ Кавказский бульвар, 57 стр. 1, Москва. Точку сдачи через API задать нельзя: склады продавца уже привязаны к офису `87609` (Кавказский). Флаг ПВЗ на задании 10.09 врал (False на 3929 заказах), решение берём с карточки поставки и из отказа на коробе.

**Файлы:** `statuses.py`, `shipments_pull.py`, `db.py`, `supply_flow.py`, `web.py`, `webui/app.js`, `webui/app.css`, `webui/index.html` (v=69), `README.md`, `_архив/test_supply.py`.

**Прод:** `72.56.8.43`, 20712 заданий WB FBS переписаны на ПВЗ Домодедовская 28; открытая поставка `WB-GI-276355488` сверена с WB: ПВЗ разрешён.

**Откат:** вернуть эти файлы из git на состояние до записи, статику на v=68, залить на прод `rsync` и пересобрать `Dockerfile.patch`.

## #08 · 17:03 · Ио

**Действие:** в окне поставки WB кнопку «Печать QR» заменил на ту же «Печать», что в общем списке. Меню: товар, отправление, отправление + грузоместо, грузоместо. Галочка стоит на любом задании, в том числе уже в коробе: печатает выбранное, состав короба не меняет. Пустой выбор — вся поставка, выделенный короб — его состав.

**Файлы:** `labels.py`, `supply_flow.py`, `web.py`, `webui/app.js`, `webui/app.css`, `webui/index.html` (v=70).

**Откат:** вернуть эти файлы, статику на v=69, залить и пересобрать `Dockerfile.patch`.

## #09 · 17:36 · Ио

**Действие:** поставку GripOn перетянул с `WB-GI-276355488` на живую `WB-GI-276491672` (ЛК, ПВЗ Домодедовская 28). Малогабарит больше не уходит на СЦ из-за флага WB: `isPickupPointShipmentAllowed` на задании и на закрытой поставке врёт. СЦ только у габарита 2/3.

**Почему ушла на СЦ:** наша `PATCH .../deliver` без тела. После неё у старой поставки `shippingPointId=null`, флаг ПВЗ False. В ЛК завели новую, выбрали «Сегодня в ПВЗ / Домодедовская 28» — API: `shippingPointId=50095011`, флаг True, `done=false`.

**Прод:** кабинет 3. Три задания `5716719759`, `5721862902`, `5717401785` и короб `WB-MP-54548686` на новой поставке, адрес ПВЗ, статус «на сборке». Старая поставка пустая, `delivered`. Бэкап `/data/ff.db.bak-20260910-pvz-refresh`.

**Файлы:** `statuses.py`, `supply_flow.py` (`refresh_from_wb`, `_sync_dropoff`), `wb_supply.py`, `db.py`, `shipments_pull.py`, `README.md`, `_архив/test_supply.py`.

**Откат:** код — вернуть эти файлы из git и пересобрать `Dockerfile.patch`. Данные — `cp /data/ff.db.bak-20260910-pvz-refresh /data/ff.db` и рестарт `web`/`worker`.

## #10 · 17:40 · Ио

**Действие:** в окне поставки WB кнопка «Сдать на ПВЗ» / «Сдать в СЦ» заменена на «На отгрузку». По нажатию тот же `PATCH .../deliver`, поставка и задания уходят во вкладку «Ожидают отгрузки». Выгрузка WB со статусом `complete` эту вкладку больше не перебивает.

**Файлы:** `db.py` (`set_wb_supply_state`, `EFF_GROUP`), `supply_flow.py` (`deliver`), `webui/app.js`, `app.css`, `index.html` (v=71), `_архив/test_supply.py`.

**Откат:** вернуть эти файлы, статику на v=70, залить и пересобрать `Dockerfile.patch`.
