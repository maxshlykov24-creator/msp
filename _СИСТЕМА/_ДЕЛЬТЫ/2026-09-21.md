# Дельты 2026-09-21

## #01 · 14:45 · Ио

**Действие:** ввод числа коробов больше не нативный спиннер. Крупные кнопки − и +, число по центру. Плюс упирается в лимит WB (половина заданий), минус в ноль или в единицу: на «Добавить короб» ноль нельзя, на «Собрано» ноль значит пропустить. Клавиатуру сами не открываем. Других полей `type=number` в панели нет.

**Файлы:** `webui/app.js`, `webui/app.css`, `webui/index.html` (v=92). Прод `194.87.27.142`, контейнер `web` пересобран.

**Прогон:** окно с максимумом 1: обе кнопки серые, число 1. С максимумом 5: плюс 1→2→3, ввод 99 сжимается до 5, плюс гаснет.

**Откат:** вернуть статику v=91, одно поле `type=number`, пересобрать `Dockerfile.patch`.
