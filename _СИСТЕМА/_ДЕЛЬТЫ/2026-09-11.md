# Дельты 2026-09-11

## #01 · 13:24 · Ио

**Действие:** в кабинет склада ff-sync добавлен логин `sereja` для Серёжи. Прав в системе нет: любой логин видит то же, что Глеб, включая счета. Прогон: `POST /api/login` по `http://berezagrp.ru` вернул ok.

**Где:** `/opt/ff-sync/.env`, переменная `FF_USERS`. Контейнер `ff-sync-web-1` пересоздан, worker не трогал.

**Копия до правки:** `/root/ff-sync.env.bak-2026-09-11-sereja` на VPS `72.56.8.43`. Пароль в vault не пишем.

**Откат:** на VPS убрать пару `sereja:…` из `FF_USERS` или вернуть файл из копии, затем `cd /opt/ff-sync && docker compose up -d --force-recreate --no-deps web`.

## #02 · 13:50 · Ио

**Действие:** точка сдачи поставки WB больше не выводится из габарита, а читается с карточки площадки. Разбор инцидента 10.09: поставка `WB-GI-276355488` уехала в СЦ, хотя у нас числилась как ПВЗ, а следующее задание легло в неё вместо пересобранной в ЛК `WB-GI-276491672`.

Проверено живыми запросами 11.09: точку через API задать нельзя вовсе. `.../supplies/{id}/shipping-point`, `/api/v3/shipping-points`, `.../spot` отвечают 404; у `PATCH .../deliver` тела нет; `/api/v3/offices` отдаёт 182 сортировочных центра и склада без единого пункта выдачи, ПВЗ Домодедовская 28 (`50095011`) там не встречается. Габарит не при чём: у всех 21000 заданий в базе `cargoType = 1`.

**Что изменилось в коде:**

- `statuses.py` — `dropoff_kind`, `dropoff`, `to_pickup`, `cargo_label` решают по `shippingPointId` и флагу с карточки; добавлены `cargo_kind` и `dropoff_warning`
- `supply_flow.py` — `_dropoff_from_card`, `_sync_dropoff` читают факт; новая `sync_open` сверяет открытые поставки кабинета с площадкой; `_open_supply` зовёт её перед отбором и больше не делит поставки по точке; `make_boxes` не отказывает по габариту; `deliver` и `preflight` перечитывают точку перед необратимым шагом
- `wb_supply.py` — новый `list_supplies` (`GET /api/v3/supplies` с пагинацией)
- `web.py` — хелперы `col` и `dropoff_of`; новая ручка `POST /api/wb/supplies/{id}/refresh`; сверка с площадкой в `/api/assembly` не чаще раза в минуту
- `db.py`, `schema.sql` — колонка `wb_supplies.shipping_point`; выгрузка больше не затирает адрес, пришедший от поставки
- `shipments_pull.py` — убрана `wb_office`: у задания вне поставки адреса нет
- `webui/` — янтарный блок предупреждения и кнопка «Обновить с площадки» в окне поставки, предупреждение в окнах подтверждения; версия статики `v=87`

**Прогон:** `_архив/test_supply.py`, `test_billing.py`, `test_picking_pdf.py` зелёные; добавлен регресс вчерашней петли. На проде `/health` ok, `GET /api/wb/supplies` подтянул три открытые поставки кабинета «ЛЕГКО ИДЁТ», которых в базе не было.

**Чистка данных:** флаг `pickup_allowed` у обеих поставок 10.09 был догадкой по габариту (`'1'`), сброшен и перечитан с площадки: `WB-GI-276355488` → СЦ, `WB-GI-276491672` → ПВЗ Домодедовская 28. У 21051 задания вне поставки адрес сдачи очищен.

**Откат:** `git revert 0889a2d` в vault, затем на VPS `cd /opt/ff-sync && docker build -f Dockerfile.patch -t ff-sync:latest . && docker compose up -d --force-recreate web worker`. Колонку `shipping_point` удалять не нужно, старый код её игнорирует. Образ до правки — `ff-sync:prev-20260911-1339`.

**Замечено, не тронуто:** в `docker-compose.yml` шапкой указан перенос на VPS `194.87.202.217` от 10.09, но прод отвечает на `72.56.8.43`, а новый адрес молчит и на 80, и на 8787. Сборка образа на VPS падает: `deb.debian.org` недоступен, поэтому собирал через `Dockerfile.patch` поверх `ff-sync:base`.

## #03 · 14:05 · Ио

**Действие:** на `berezagrp.ru` и `www.berezagrp.ru` выпущен Let's Encrypt (DNS-01). HTTP-01 не прошёл: с США и Европы до `72.56.8.43` таймаут, с Москвы сайт открывается. Сертификат скопирован в nginx edge, продление через hook.

**Где:** VPS `72.56.8.43`, `/etc/letsencrypt/live/berezagrp.ru/`, `/opt/ff-sync/edge/certs/{cert,key}.pem`, hook `/etc/letsencrypt/renewal-hooks/deploy/ff-sync.sh`. TXT `_acme-challenge` и `_acme-challenge.www` в DNS Timeweb.

**Прогон:** `curl https://berezagrp.ru/health` и `https://www.berezagrp.ru/health` → 200, `ssl_verify=0`. Срок до 2026-12-10.

**Откат:** вернуть прошлые `cert.pem`/`key.pem` в `/opt/ff-sync/edge/certs/` и `docker compose exec -T edge nginx -s reload`. Сами файлы LE не удалять, пока не истечёт срок.

## #04 · 15:20 · Ио

**Действие:** кабинет Берёзы выведен наружу через живой VPS кассы `72.56.240.103`. Нода `72.56.8.43` в аварии Timeweb, с мира не открывается. На кассовом nginx добавлен сайт `berezagrp`, прокси на `http://72.56.8.43:80`. Кассу не менял.

**Где:** `/etc/nginx/sites-available/berezagrp` на `72.56.240.103`, сертификаты `/etc/nginx/certs/berezagrp/` (копия с origin) и Let's Encrypt на `72.56.240.103.sslip.io`.

**Прогон:** `https://72.56.240.103.sslip.io` → 200, check-host 12/12 OK. `https://mansband-kassa.ru` → 200.

**Откат:** на кассовом VPS `rm /etc/nginx/sites-enabled/berezagrp && nginx -t && systemctl reload nginx`.
