# Дельты 2026-09-16

## #01 · 12:55 · Ио

**Действие:** ff-sync перенесён с больной ноды `72.56.8.43` на `194.87.27.142` (хост `berezagrp`). Склад открывает панель по `https://194.87.27.142.sslip.io/` — валидный Let's Encrypt до 2026-12-15, HTTP-01 прошёл сам, продление через `certbot.timer`.

**Почему:** старая нода снаружи не маршрутизируется (15.09: 2 узла из 16). Сертификат на ней был, складу это не помогало. Новая нода: HTTPS 13/16 на check-host (отказы только из Ирана).

**Что на новом:** Docker, образ, база 28 МБ, web+edge+worker. 5 клиентов, воркер ходит в МойСклад. Кассовый край `72.56.240.103` переключён на новый origin. На старом все три контейнера остановлены, данные не трогал.

**Прогон:** `/health` 200, UI логина 200, `POST /api/login` 401 на заведомо неверной паре, сертификат `ssl_verify=0`.

**Откат:** на старом `cd /opt/ff-sync && docker compose up -d`; на кассе вернуть upstream `72.56.8.43` из `/root/berezagrp.nginx.bak-*`.
