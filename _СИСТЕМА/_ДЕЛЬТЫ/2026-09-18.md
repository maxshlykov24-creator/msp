# Дельты 2026-09-18

## #01 · 14:23 · Ио

**Действие:** в фулфилмент заведён клиент ИП Андреева Ольга Валерьевна, бренд ДомаИдеал. Реквизиты: ИНН 504300179920, ОГРНИП 322508100305236 (ЕГРЮЛ ФНС, действующая запись от 16.06.2022). Токен WB принят, в карточке МойСклад маска.

**Где:** МойСклад `1c5bfb66-b353-11f1-0a80-1af5002c9beb`, панель `IPANDREEVAOL` id=9, кабинет WB id=11 active. Каталог 119 карточек. Отправления FBS за 14 дней: 0. FBO не снял: у ключа нет scope статистики, WB 403.

**Откат:** снять тег «фулфилмент» с контрагента и выключить кабинет WB id=11.

## #02 · 14:28 · Ио

**Действие:** в «Сборке» снял потолок 100 строк. Галка в шапке отмечает все задания текущей вкладки с учётом фильтра (контрагент, площадка, FBS/FBO, поиск, период), а не первую сотню. Счётчик вкладки и таблица совпадают: на проде «Новые» 670 из 670.

**Файлы:** `web.py`, `webui/app.js`, `webui/index.html` (v=90). Прод `194.87.27.142`, контейнер `web` пересобран.

**Прогон:** `_архив/test_supply.py` — 120 новых, `list_assembly` без лимита отдаёт 120, с `limit=100` — 100, чужой артикул пустой. Живая база: `new_rows == tab`.

**Откат:** вернуть `limit=100` в `/api/assembly` и статику v=89, пересобрать `Dockerfile.patch`.
## #03 · 18:52 · Ио

**Действие:** точка сдачи поставки WB выбирается прямо в панели, в личный кабинет за этим ходить не нужно. Ставится сама при создании поставки (по умолчанию ПВЗ Домодедовская 28), меняется кнопкой «Сменить точку» в окне поставки с поиском по адресу. Прежний вывод «через API точку не задать» опровергнут: методы лежат на префиксе `/api/marketplace/v3/fbs/`, а 11.09 их искали на `/api/v3/`.

**Файлы:** `wb_supply.py` (`shipping_points`, `set_shipping_method`, `_ask`), `supply_flow.py` (`points`, `set_dropoff`, `ensure_dropoff`, `dropoff_default`, `point_fits`), `db.py` и `schema.sql` (таблица `wb_points`, колонка `wb_supplies.shipping_dt`), `statuses.py` (адрес от точки, новый текст предупреждения), `web.py` (`GET /api/wb/shipping-points`, `POST /api/wb/supplies/{id}/dropoff`), `webui/app.js`, `webui/app.css`, `webui/index.html` (v=91), `README.md`, `_архив/test_supply.py`. Прод `194.87.27.142`, контейнеры `web` и `worker` пересобраны.

**Прогон:** живые запросы к WB — `shipping-points` по Москве и `cargoType=1` отдал 5567 точек (5565 ПВЗ, 2 СЦ), среди них `50095011`; `PATCH shipping-method` на `WB-GI-279676408` вернул `success: true`, в карточке появились `shippingPointId=50095011`, `shippingDt`, `shippingType=selfShipping`. Из 14 открытых поставок точку получили 12: `WB-GI-275521331` — `NotFound`, такой поставки на площадке нет, `WB-GI-195597308` от 14.11.2025 — пустая, без заданий. Через HTTP панели: карточка показывает адрес и дату, смена точки 28 → 33 → 28 принята площадкой. В браузере: секция «Куда везти», поиск по улице отдаёт 9 точек, сообщение «Везём: Москва, Домодедовская Улица 28». `_архив/test_supply.py` — все проверки прошли, добавлен блок 27 на точку сдачи, включая обрыв связи и отсканированную поставку.

**Откат:** вернуть статику v=90, убрать вызовы `ensure_dropoff` из `_open_supply`, `create_supply`, `add_orders` и `preflight`, снять эндпоинты точек в `web.py`. Таблицу `wb_points` и колонку `shipping_dt` можно оставить: они только читаются. Уже выбранные точки на площадке останутся, снять их через API нельзя.
