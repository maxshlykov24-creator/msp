# Дельты 2026-09-15

## #01 · 11:08 · Ио

**Действие:** в фулфилмент заведён клиент ИП Пахомова Елена Петровна (бренд WildScreen). Реквизиты сняты с WB ЛК: ИНН 183310461106, ОГРНИП 326180000029489, адрес Ижевск. Токен WB принят, в карточке МойСклад уже маска. Ozon ключа не было.

**Где:** МойСклад контрагент `f18ef83b-b0db-11f1-0a80-07c300202cc4`, панель `IPPAHOMOVAEL` id=4, кабинет WB id=5 active. Каталог: 1 карточка. Отправления за 14 дней: 29 (11 отгружены, 12 доставлены, 6 отменены). Живой сборки сейчас нет.

**Откат:** снять тег «фулфилмент» с контрагента в МойСклад и выключить кабинет WB id=5. Токен в панели не светится, лежит только в базе кабинета.

## #02 · 11:12 · Ио

**Действие:** заведён клиент ООО «СОРТО» (Химки, ИНН 7728446957) с двумя кабинетами Ozon: Client-Id 2538994 (SAVOUR HOME, 515 карточек) и 49170 (Little Crown, 306 карточек). Ключи пингуются, в карточке МойСклад маска первого. WB не было. FBO-роль у ключей нет, FBS живой.

**Где:** МойСклад `13daeaa9-b0dd-11f1-0a80-08e00022d7f4`, панель `OOOSORTO` id=5, кабинеты id=6 и id=7. За 14 дней у 2538994: 28 отправлений, из них 2 ждут отгрузки. У 49170 отправлений за окно нет.

**Откат:** снять тег «фулфилмент», выключить кабинеты 6 и 7. Второй ключ в МойСклад не клался: в карточке одно поле Ozon, второй кабинет только в базе панели.

## #03 · 16:15 · Ио

**Действие:** 502 на berezagrp.ru. Nginx edge держал старый IP контейнера web (`172.18.0.3`) после recreate 11.09 без edge. Сам uvicorn на `:8787` был жив, домен шёл в никуда. Перечитал DNS Docker и поправил `edge/nginx.conf`: резолвер `127.0.0.11`, `proxy_pass` через переменную, чтобы IP web не залипал.

**Прогон:** `/health` с origin 80/443, с кассы и снаружи по HTTP = 200.

**Откат:** вернуть `proxy_pass http://web:8787;` в `/opt/ff-sync/edge/nginx.conf` и `docker compose exec edge nginx -s reload`.

## #04 · 19:20 · Ио

**Действие:** разобрана настоящая причина «на складе сайт не открывается». Нода прода `72.56.8.43` снаружи не маршрутизируется: замер check-host 15.09 — HTTP до домена прошёл с 2 узлов из 16, TCP 443 тоже 2 из 16, московский узел `ru3` не дошёл. Контейнеры, `/health` и сертификат при этом живые. Держится с 11.09, это сеть Timeweb, не наш код. Живой VPS `72.56.240.103` доступен со всех 16 узлов.

**Что сделано на краю** (`72.56.240.103`, `/etc/nginx/sites-available/berezagrp`, копия в vault `edge/kassa-berezagrp.conf`):

- два upstream: основной `72.56.8.43:80`, запасной `72.56.8.43:8787` напрямую в uvicorn, `proxy_next_upstream` по 502/503/504 и таймауту
- `connect_timeout 5s` вместо 10, страница «Кабинет перезапускается» вместо голого 502
- webroot `/var/www/certbot` и hook `renewal-hooks/deploy/reload-nginx.sh` — под автопродление после смены DNS
- скрипт выпуска `/root/cert-berezagrp.sh` (в vault `edge/kassa-cert-berezagrp.sh`): сверяет, что A-записи смотрят на край, иначе не тратит попытку в лимите Let's Encrypt

**Прогон на живом входе:** погасил `edge` на проде целиком — панель через край продолжала отдавать 200 (`/health` и UI), после `start` вернулась на основной upstream. Логин `POST /api/login` через край отвечает логикой приложения (401 на заведомо неверной паре), статика `v=89` отдаётся. Касса MANSBAND после reload — 200, её конфиг не тронут. Подстановка `--resolve berezagrp.ru:443:72.56.240.103` с третьей машины: 200, `ssl_verify=0` — после смены DNS домен откроется с действующим сертификатом.

**Осталось за владельцем:** A-записи `berezagrp.ru` и `www` в панели Timeweb с `72.56.8.43` на `72.56.240.103`. TTL 300 секунд. Панельного логина Timeweb в vault нет, поэтому сам не делаю.

**Откат:** `cp /root/berezagrp.nginx.bak-20260915_160852 /etc/nginx/sites-available/berezagrp && nginx -t && systemctl reload nginx`. A-записи вернуть на `72.56.8.43`.
