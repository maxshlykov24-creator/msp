# Дельты 2026-08-24

## #01 · 12:45 · Ио

**Действие:** добавочный 101 LicenseBridge передан Павлу (владелец) вместо уволенной Илоны, её софтфон отключён. Пароль 101 сменён `Manager101pass2026` → `Owner101pass0824` в `/etc/asterisk/pjsip_wizard.conf`; `pjsip reload` и `module reload res_pjsip_config_wizard.so` пароль не применяют, поэтому сделан `core restart when convenient` при нуле активных каналов (09:41 UTC, 02:41 по Калифорнии). Контакт Илоны с IP `95.55.248.87` (MicroSIP, Калининград, 38 перерегистраций за сутки) ушёл по истечении срока, новых регистраций с него нет. Диалплан: `LB_ORDER_FALLBACK=103-101`, финальный `Dial` в `sales` → `103&101`, добавлен тестовый `8874` (исходящий с CallerID 101). Хаб: `TELEPHONY_EXT_USERS` += `101:13291175`, `TELEPHONY_SALES_ORDER=103,101`, `TELEPHONY_DID_EXT=1:101,3:103`, контейнеры пересозданы (`restart` новый `.env` не читает). Возврат линии 1 Александре — таймер `lb-pavel-off.timer` на 2026-08-25 06:00 UTC = 23:00 по Калифорнии. Проверено на живом: роут `did=1 → 101-103`, `did=3 → 103-101`; исходящий 8874 — разговор 40 с, `cid=+18187911783`, `ext=101` доехал до хаба, решение `call.no_card`, deadletter 0.

**Файлы:**
- `2. БИЗНЕС/04_Производство/Активные/LicenseBridge_USA/asterisk/extensions.conf` — шапка ролей, `LB_ORDER_FALLBACK`, финальный `Dial`, тест `8874`
- `.../asterisk/lb-trunk-watch.sh` — комментарий к `ALERT_EXTS` (101 офлайн по графику, не отвал); синхронизирован на сервер
- `.../tilda-webhook/app/config.py` — дефолты карты телефонии и комментарии
- `.../tilda-webhook/RUNBOOK.md` — раздел «Линия на одну смену»
- `.../ОПЫТ_ТЕЛЕФОНИЯ_ASTERISK_TG400.md` — §0 роли, новый §7а о смене пароля добавочного
- `.../ДОСТУПЫ.md` — добавочные и DID
- `.../ДОСТУПЫ_MicroSIP_SIM1.md` — переписан под Павла, новый пароль и текст для отправки
- `.../инструкция/MicroSIP_SIM1.md` — роль, поведение офлайн, TCPA

**Откат:** Asterisk — `cp /etc/asterisk/pjsip_wizard.conf.bak-20260824-093510-pavel /etc/asterisk/pjsip_wizard.conf` плюс перезапуск (вернёт прежний пароль и доступ Илоны), диалплан — `extensions.conf.bak-20260824-124013` или `./deploy.sh` из git. Хаб — `cp .env.bak-20260824-093842-pavel .env && docker compose up -d --force-recreate api worker`. Таймер снять: `systemctl stop lb-pavel-off.timer`, скрипт `/usr/local/bin/lb-pavel-off.sh` удалить.

**Не сделано, требует владельца (по состоянию на #01):** доступ Илоны в Kommo (`15537380`) остаётся активным — API Kommo менять пользователей не умеет (`PATCH /users` → 405), снимается только руками в интерфейсе. Сущностей на ней ноль (сделки, контакты, задачи), риска при отключении нет.

## #02 · 15:32 · Ио

**Действие:** уточнён график смен LicenseBridge (Александра 09:30–15:00, Павел 16:00–21:00 по LA). Одноразовый таймер `lb-pavel-off` снят, скрипт `/usr/local/bin/lb-pavel-off.sh` удалён. Вместо него на хабе `/usr/local/bin/lb-line1-owner.sh pavel|sasha` — два состояния линии 1: `pavel` (`TELEPHONY_DID_EXT=1:101,3:103`, `SALES_ORDER=103,101`) и `sasha` (`1:103,3:103`, `SALES_ORDER=103`). Оба состояния прогнаны на живом роуте: `pavel` → `did=1: 101-103`, `did=3: 103-101`; `sasha` → `did=1: 103`, `did=3: 103`. Сейчас активно `sasha` (в LA 05:30, смена Александры). Таймеры: `lb-line1-pavel` на 24.08 23:00 UTC = 16:00 LA, `lb-line1-sasha` на 25.08 04:00 UTC = 21:00 LA. Переключения пишутся в `/var/log/lb-line1-owner.log`. По решению владельца номер +18188066735 остаётся входящим, исходящие Александры продолжают идти с +18188066005 — ротацию DID не делаем; новый номер под продажи покупаем позже.

**Файлы:**
- `2. БИЗНЕС/04_Производство/Активные/LicenseBridge_USA/tilda-webhook/RUNBOOK.md` — раздел «Линия на одну смену» переписан под два состояния и таймеры границ
- `.../ДОСТУПЫ_Telnyx.md` — таблица DID: кто владелец каждого номера, где стоит подмена, на каком номере метка

**Откат:** `/usr/local/bin/lb-line1-owner.sh sasha` возвращает линию Александре немедленно. Снять таймеры: `systemctl stop lb-line1-pavel.timer lb-line1-sasha.timer`. Бэкапы `.env` — `.env.bak-*-line1-*` в `/opt/licensebridge-tilda-webhook`.
